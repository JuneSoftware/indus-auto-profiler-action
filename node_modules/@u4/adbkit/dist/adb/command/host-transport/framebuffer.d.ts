/// <reference types="node" />
/// <reference types="node" />
import Command from '../../command';
import { Readable } from 'stream';
import FramebufferMeta from '../../../models/FramebufferMeta';
import FramebufferStreamWithMeta from '../../../models/FramebufferStreamWithMeta';
export default class FrameBufferCommand extends Command<FramebufferStreamWithMeta> {
    execute(format: string): Promise<FramebufferStreamWithMeta>;
    _convert(meta: FramebufferMeta, format: string, raw?: Readable): Readable;
    _parseHeader(header: Buffer): FramebufferMeta;
}
//# sourceMappingURL=framebuffer.d.ts.map